// este archivo hace el import de dotenv/config
// y un import del index.ts

// vamos a usar una libreria llamada dotenv, esta liberia automaticamente lee un archivo .env que esta en nuestro repo
import "dotenv/config";
import "./index.ts";
